# 11 — SEO Specification: Nexora Prism

## 1. Title & Meta Directives
- Title: `Prism — Performance Refracted`
- Viewport: `width=device-width, initial-scale=1.0, viewport-fit=cover`
- Theme Color: Dynamic update script (`#050505` dark / `#f2f0eb` light)

## 2. Semantic HTML & Typography Links
- Google Font preconnects (`fonts.googleapis.com`, `fonts.gstatic.com`).
- Single `<h1>` tag per route view with proper heading hierarchy (`h1` -> `h2` -> `h3`).
